"use server";

export async function setUserActive(
  userId: string,
  ativo: boolean
): Promise<{ success?: boolean; error?: string }> {
  throw new Error("[STUB] T092 — pending implementation");
}
